node_modules/
dist/
out/
release/

# napi-rs build artifacts
native/target/
native/*.node
native/npm/

# Electron
.vite/

# Twemoji JSON bundle — generated from @twemoji/svg npm package by
# scripts/build-twemoji-map.cjs on every `npm run dev` / `npm run build`.
src/components/emoji/twemoji-data.json
# Old copy-twemoji output (kept ignored so existing checkouts don't
# accidentally commit ~8 MB of SVGs that are no longer used).
public/twemoji/

# Sentry DSN baked by CI on release builds — never tracked.
resources/sentry.json
